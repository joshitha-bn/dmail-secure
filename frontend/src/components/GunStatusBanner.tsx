"use client"

// Banner removed — GunDB connects automatically via WebSocket in the background.
// Showing scary network errors for slow relay connections creates bad UX.
// The sidebar NetworkStatus component provides subtle status info if needed.

export default function GunStatusBanner() {
    return null
}
